# HEAR
A Node.js horror maze prototype.
Run:
npm install
npm start
Open http://localhost:3000

Render: create a Web Service from this repository, build command `npm install`, start command `npm start`.
